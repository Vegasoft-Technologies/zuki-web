# The recovered production site is a byte-exact record of what the live server serves.
# It is moved, never reformatted.
reference/

# The application stylesheet is that same recovered file, moved into place. Reformatting
# it would silently rewrite the one artefact the migration is measured against.
src/app/globals.css

# Generated or vendored
.next/
out/
build/
coverage/
node_modules/
package-lock.json
